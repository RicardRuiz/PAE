/*
 * lib_controllers.h
 *
 *  Created on: 2 d’abr. 2019
 *      Author: mat.aules
 */

#ifndef LIB_CONTROLLERS_H_
#define LIB_CONTROLLERS_H_

#include "UART_S.h"
/*
void move_forward(void); // Move the robot forward indefinitely at current speed
void turn_right(void); // Turns the robot right indefinitely at current speed
void turn_left(void); // Turns the robot left indefinitely at current speed
void move_backward(void); // Move the robot backward indefinitely at current speed
void stop_movement(void); // Stops the movement, whichever it is
void change_speed(uint16_t speed); // Changes the speed for all the other movements
void switch_led(uint8_t id, uint8_t value); // Switches the led on or off of the given id
void spin_right(void); // Spins the robot right indefinitely at current speed
void spin_left(void); // Spins the robot left indefinitely at current speed
void abstract_movement(uint16_t speed[2], uint8_t direction[2]); // Sets an abstract movement with the given velocity for each motor
                                                                 // and the provided turning direction.
uint8_t* getDistanceSensor(void); // Returns the a vector with the distance from the front, right and left of the robot
*/
#endif /* LIB_CONTROLLERS_H_ */
